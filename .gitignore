node_modules/
dist/
.DS_Store
*.local

# copyrighted reference material — research only, never ship or publish
manual-death rally.pdf
Screenshot*.png

# before/after captures from UI refactor passes
.shots/

# Superpowers brainstorm companion (local mockups, do not commit)
.superpowers/

# Project B source car art (green-screen renders) — research/local, never commit.
# Anchored to repo root so it does NOT match public/assets/cars/ (shipped WebP).
/cars/
/.playwright-cli/
